# App

## Run app

```sh
poetry install
poetry run start
```

## Run linter

```sh
poetry run lint
```

## Run tests

Single run:

```sh
poetry run test
```

CLI in watch mode:

```sh
poetry run test_watch
```

From VSCode: <kbd>Ctrl + ; A</kbd> (all tests)
